{
  "name": "lihemobilesite",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd react_template && npm install && npm run dev",
    "build": "cd react_template && npm install && npm run build",
    "preview": "cd react_template && npm run preview",
    "install": "cd react_template && npm install"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 